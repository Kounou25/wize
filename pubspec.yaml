name: wize_writter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  audioplayers: ^5.2.1
  flutter:
    sdk: flutter
  flutter_nfc_kit: ^3.6.2
  http: ^1.6.0
  ndef: ^0.4.0
  flutter_secure_storage: ^9.0.0 

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/tons/beep.mp3

